# Backend built for Dating application Shoty

#### Used packages:

- Apollo server
- Graphql
- Prisma (mongodb)

#### Available features:

- linting (eslint)
- testing (jest)
- generating GraphQL types (graphql-codegen)
- generating types from Prisma (prisma:generate)
